Создадим поле точек размером 10 х 10 (площадью 100), разбитый горизонтальной осью х и вертикальной у
Нарисуем квадрат с координатами х5у5, х5у7, х7у7,х7у5.
Создадим функцию fillarea с аргументами 'a', 'x', 'y', в которых будут находиться переменные
с данными:
 'а' - переменная со значениями цвета в формате RGB 
 'х' - переменная со значениями координаты по оси х
 'у' - переменная со значениями координаты по оси у 
Создадим цикл со следующими условиями:
1. Если х меньше 0, или у меньше 0, или х больше или равно 
размерности переменной а с индексом первого элемента [0], то :
Цикл вернет 0
Переменная val примет значение а (цветовой код в трехзначном формате RGB).
Далее, если первая часть цикла не выполнится в силу не удовлетворения условий цикла, запустится вторая часть:
2. Если в переменной val с индексом 0 (1 число - красный цвет), элемент равен 255 то :
Цикл вернет 0
Переменная а примет значение 255.0.0
Переменная area примет значение 1
Далее, если вторая часть цикла не выполнится в силу не удовлетворения условий цикла, запустится третья часть:
3. Для переменной _у в диапазоне от -1 до 2 (то есть -1,0,1):
        И для переменной _х в диапазоне от -1 до 2 (то есть -1,0,1):
         Будет найдена точка с цветом начинающимся не на 255 то :
         Переменная area примет значение суммарное самой себе + переменных (а, х + _х, у + _у)
Цикл вернет значение area 