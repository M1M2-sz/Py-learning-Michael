Описание алгоритма :
Представим что мы хотим посчитать площадь квадрата который находится 
на центральных координатах х5у5 и имеет длинну стороны 3 точки.
Создаем таблицу, в которой есть колонки с значениями:
x = 5          - абсолютная координата по х 
y = 5          - абсолютная координата по у  
_y = -1,0,1    - _у в диапазоне (-1,2) представляет собой -1,0,1
_x = -1,0,1    - _х в диапазоне (-1,2) представляет собой -1,0,1
NewX = x + _x  - новое значение х как результат шага цикла 
NewY = y + _y  - новое значение у как результат шага цикла 

Step|x|y|_x|_y|NewX|NewY|
| 1 |5|5|-1|-1|  4 |  4 |
| 2 |5|5| 0|-1|  5 |  4 | 
| 3 |5|5| 1|-1|  6 |  4 |
| 4 |5|5|-1| 0|  4 |  5 |
| 5 |5|5| 0| 0|  5 |  5 |
| 6 |5|5| 1| 0|  6 |  5 |
| 7 |5|5|-1| 1|  4 |  6 | 
| 8 |5|5| 0| 1|  5 |  6 |
| 9 |5|5| 1| 1|  6 |  6 |

Таким образом мы прошли цикл х вложенный в цикл у по 3 раза с каждой
сменой итерации шага по у.
Мы получаем за каждый Step координаты новой точки.
Эти координаты удаляют (или добавляют если заливка) точки на плоскость.
То есть мы нашли 9 координат(по step), соотв 9 точек на карте и их удалили(или добавили если заливка)

Описание методов.
Метод 1

count=0
# обнуляем переменную count
for y in range(im.height):# для y в диапазоне (im.по высоте)
 for val in a[y]:# для val в a с индексом [y]
  if (val[0]!=255):# если индекс 0 переменной val [red] не равен 255 то :
   count+=1# счетчик +1
print('area =',count)# выводим строку и значение count

Метод 2 с внутренним циклом х 
count=0
# обнуляем переменную count
for y in range(im.height):# Для y в диапазоне (im.по высоте)
 for x in range(im.width):# Для х в диапазоне (im.по длинне)
  val=a[y,x]# переменная val = индексы a [y,x]
  if (val[0]!=255):# если индекс 0 переменной val [red] не равен 255 то :
   count+=1# счетчик +1
print('area =',count)# выводим строку и значение count

Метод 3 с внутренним циклом х 
count=0
# обнуляем переменную count
totalarea=0
# обнуляем переменную totalarea - общая площадь 
# создаем двойной цикл
for y in range(im.height): # Для y в диапазоне (im.по высоте)
 for x in range(im.width): # Для х в диапазоне (im.по длинне)
  val=a[y,x] # переменная val = индексы a [y,x]
  if val[0]!=255: # если индекс 0 переменной val [red] не равен 255 то :
   count+=1 # счетчик +1
   area=fillarea(a,x,y) # переменной area присваиваем значения от fillarea а именно a, x, y
   print(' i have found ',count,' figure with area = ',area) # выводим строку и значение count, потом еще строку и значение area
   totalarea+=area # к переменной totalarea добавляем значение от area
print('total area is ',totalarea) # выводим строку и значение totalarea